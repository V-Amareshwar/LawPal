services:
  - type: web
    name: lawpal-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeploy: true
    # healthCheckPath: /auth/health
    # Set environment variables in Render dashboard or via secrets
    # envVars:
    #   - key: NODE_ENV
    #     value: production
    #   - key: MONGODB_URI
    #     fromSecret: lawpal-mongodb-uri
    #   - key: JWT_SECRET
    #     fromSecret: lawpal-jwt-secret
    #   - key: FRONTEND_URL
    #     value: https://your-frontend.vercel.app
    #   - key: GROQ_API_KEY
    #     fromSecret: lawpal-groq-api-key
    #   - key: SMTP_HOST
    #     value: smtp.gmail.com
    #   - key: SMTP_PORT
    #     value: "587"
    #   - key: SMTP_USER
    #     fromSecret: lawpal-smtp-user
    #   - key: SMTP_PASS
    #     fromSecret: lawpal-smtp-pass
    #   - key: SMTP_SECURE
    #     value: "false"
    #   - key: MAIL_FROM
    #     value: "LawPal <noreply@yourdomain.com>"
