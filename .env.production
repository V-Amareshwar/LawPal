# Production environment (Vercel)
# Do NOT set VITE_API_BASE_URL here - production uses relative /auth paths
# which get proxied to Render via vercel.json rewrites

# Chat inference endpoint
VITE_CHAT_ENDPOINT=/ask
